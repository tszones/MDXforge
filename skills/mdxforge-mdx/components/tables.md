# Tables / TypeTable

Use Markdown tables for simple tabular content. Use `TypeTable` for API/type reference sections when the prop schema is known.

```mdx
| Prop | Type | Description |
| --- | --- | --- |
| title | string | Display title. |
```

Keep tables readable on narrow screens; split very wide tables into sections.
