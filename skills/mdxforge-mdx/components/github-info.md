# GitHub Info

Use `GithubInfo` for concise repository metadata when a GitHub project is directly relevant.

```mdx
<GithubInfo owner="owner" repo="repo" />
```

If repo props are uncertain, use a normal Markdown link instead.
