# Inline TOC

Use `InlineTOC` when a long document needs an in-content navigation summary near the top.

```mdx
<InlineTOC items={toc} />
```

If the TOC data shape is uncertain, use normal headings and let MDXForge generate the page table of contents.
