# Images / ImageZoom

Use `ImageZoom` for local screenshots, diagrams, or product images that benefit from zooming.

```mdx
<ImageZoom src="./screenshot.png" alt="Settings page" />
```

Always provide meaningful alt text. Prefer local or explicitly trusted image paths.
