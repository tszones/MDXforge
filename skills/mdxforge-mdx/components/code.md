# Code / DynamicCodeBlock / Twoslash

Use fenced code blocks for most examples. Use `DynamicCodeBlock` or Twoslash-powered code only when syntax highlighting, annotations, or TypeScript diagnostics add real value.

````mdx
```ts
export const value = 1
```
````

Keep examples copyable. Avoid executing arbitrary HTML or scripts.
