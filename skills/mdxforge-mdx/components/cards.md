# Cards / Card

Use cards for related docs, choices, or resource links.

```mdx
<Cards>
  <Card title="Quick Start" href="https://example.com">
    Read the guide.
  </Card>
</Cards>
```

`Card` should have a `title`. Put `Card` inside `Cards`.
