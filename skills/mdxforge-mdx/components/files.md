# Files / File / Folder

Use file tree components for project layout examples.

```mdx
<Files>
  <Folder name="docs">
    <File name="index.mdx" />
  </Folder>
</Files>
```

Prefer fenced `txt` trees when component props are uncertain.
