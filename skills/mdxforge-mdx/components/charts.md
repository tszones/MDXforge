# Metrics and Charts

Available chart/data components include `MetricCard`, `StatGrid`, `SimpleBarChart`, and `SimpleLineChart`.

Use only with small, explicit data. If props are uncertain, render the data as a Markdown table.
