# Tabs

Use tabs for equivalent variants such as package managers or platforms.

```mdx
<Tabs items={["pnpm", "npm"]}>
  <Tab value="pnpm">Use pnpm.</Tab>
  <Tab value="npm">Use npm.</Tab>
</Tabs>
```

If exact tab API is uncertain, use headings instead.
