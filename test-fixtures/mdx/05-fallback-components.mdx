---
title: Fallback Components
description: Allowed placeholder components that should not crash rendering.
---

# Fallback Components

These components are allowed for compatibility but currently render placeholders.

<Accordions>
  <Accordion title="Fallback accordion">
    This content should render inside a fallback box.
  </Accordion>
</Accordions>

<Steps>
  <Step>
    Open a local MDX file.
  </Step>
  <Step>
    Render it through Docuforge.
  </Step>
</Steps>

<Files>
  <File name="package.json" />
  <File name="docs/roadmap.mdx" />
</Files>

<Tabs>
  <Tab title="One">First tab content.</Tab>
  <Tab title="Two">Second tab content.</Tab>
</Tabs>
