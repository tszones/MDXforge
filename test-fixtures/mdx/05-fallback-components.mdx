---
title: Fallback Components
description: Allowed placeholder components that should not crash rendering.
---

# Fallback Components

These components are rendered by the built-in Fumadocs UI component set.

<Accordions>
  <Accordion title="Fallback accordion">
    This content should render inside a fallback box.
  </Accordion>
</Accordions>

<Steps>
  <Step>
    Open a local MDX file.
  </Step>
  <Step>
    Render it through MDXForge.
  </Step>
</Steps>

<Files>
  <File name="package.json" />
  <File name="docs/roadmap.mdx" />
</Files>

<Tabs items={["One", "Two"]}>
  <Tab>First tab content.</Tab>
  <Tab>Second tab content.</Tab>
</Tabs>
